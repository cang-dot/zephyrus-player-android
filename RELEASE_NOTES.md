# Zephyrus Player 更新日志

## v1.3.0 · 2026-08-30

> 要开学了，最后一舞。。。

### 新增

- 全新「错误」播放器样式：黑底高速流体（歌曲主题色纹理）、CRT 扫描线、老电视颗粒噪点、大字宋体歌词溶解进出，高潮段全屏错误爆发（负片闪、撕裂、色散、故障块）并随强拍触发；首次启用提供光敏性癫痫警告。
- 四个主界面改为常驻 pager：横滑时当前页与相邻页 1:1 双页跟手，松手按速度弹簧滑入目标页，切换全程无空白；每页独立记忆滚动位置。
- 播放器打开/关闭与全局弹簧进度打通，下滑关闭跟手变暗淡出，可随时反向。
- 界面顶栏胶囊在页面切换时收缩/展开过渡；全屏播放器与列表确认、下载设置、本地音乐文件夹管理等浮层统一为玻璃质感。
- 扫歌词海报二维码的中继页支持「用网易云音乐打开」；网易云分享链接可在微信「用其他软件打开」中选择 Zephyrus 直接弹出歌曲。
- 播放设置支持云端歌词 AI 模型选择与积分展示。

### 修复

- 修复切换播放器样式时配置被旧快照覆盖导致"点了没反应"、重开后回退旧样式的问题。
- 修复滑动切页在触摸设备上被垂直滚动误中断、切页后跳回首页的问题。
- 修复底部控件在打开并收起面板后悬停在屏幕中部的问题（自动收起与共享容器下移冲突）。
- 修复烟雾样式取色偏白：封面主色强制提升饱和度。
- 修复滚动歌词开关与间奏时主歌词短暂错位，段落头尾改为溶解过渡。
- 修复下载页与设置抽屉的桌面形态弹层在移动端的观感。
- 清理 42 个不可达的桌面遗留组件（约 1.86 万行）。

### 其他

- 引入 three.js 流体背景组件（仅「错误」样式按需加载，30fps 上限、半分辨率模拟、减弱动画时静止）。
- 二级页 keep-alive 命中修正（歌单/发现页此前从未被缓存）。

## v1.2.5 · 2026-08-21

这次发布把移动播放器、歌词和云端歌词 AI 收拢到同一套可验证的状态流中，重点是减少长时间运行后的卡顿和误触发。

### 新增

- 重写移动播放界面的展开、收起、滚动歌词和播放设置过渡，支持可中断的跟手手势与系统返回。
- 默认样式统一使用 AMLL Vue 滚动歌词组件，保留 TTML 背景声部、翻译、罗马音、逐字时间轴和歌词选择。
- 歌词设置加入云端 AI 模型选择、每日积分、倍率、流式 Markdown 输出和一键复制纯文本。
- 播放设置支持智能过渡、无缝切歌和更轻量/更智能的连续调节。
- 文档站与 Android 产品介绍站重写，补充安装、歌词来源、播放器样式、账号与故障排查入口。

### 修复

- 修复进度跳转或快速切歌后错误显示暂停的问题，旧音频事件不会再恢复错误的播放状态。
- 修复滚动歌词点击空白、长按选择和左右滑动在不同播放器样式下失效的问题。
- 修复背景歌词时间轴与主歌词错误共用时间、背景动画被歌词覆盖层暂停的问题。
- 修复默认样式封面、在线媒体封面和歌曲过渡阶段偶发不同步的问题。
- 修复播放器设置页横向分页、下拉模型选择和自定义样式内容溢出。
- 修复本地歌曲快速连续切换时重复提示“解析失败”的队列状态。

### 安全与兼容

- 部署脚本改为从环境变量读取 SSH 密码和 GitHub Token，仓库不再保存凭据。
- Android `versionCode` 更新为 8，`versionName` 与前端、文档统一为 `1.2.5`。
- Spotify 登录在未配置开发者应用或用户不在白名单时明确提示限制，不显示伪造的登录成功状态。

## v1.2.0

- 完成 Android 移动播放器重构、原生状态栏歌词、AMLL/TTML 歌词支持、本地音乐和多平台账号基础能力。
- 发布 GitHub Actions Android Release 工作流和服务器 APK 直链。

## v1.1.x 及更早版本

历史版本记录保留在 Git 历史中；安装与迁移说明请以[文档站](https://mucang.xyz/zephyrus/docs/)为准。
